import { Comfortaa, Geologica, Russo_One } from "next/font/google";

export const comforta = Comfortaa({
  subsets: ["cyrillic"],
  display: "swap",
});

export const russo = Russo_One({ subsets: ["cyrillic"], weight: "400" });
